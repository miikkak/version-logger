---
name: Gradle Lockfile Refresh

on: # yamllint disable-line rule:truthy
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  refresh-lockfile:
    name: Gradle Lockfile Refresh
    uses: miikkak/workflows/.github/workflows/gradle-lockfile-refresh.yml@b847a1b579129b0f1d45456ffb2b1a054f9f406c # main
    secrets:
      PULL_REQUEST_TOKEN: ${{ secrets.PULL_REQUEST_TOKEN }}
