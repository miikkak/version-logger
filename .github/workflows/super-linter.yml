---
name: Lint

on: # yamllint disable-line rule:truthy
  push:
    branches: [main]
  workflow_dispatch:


permissions:
  contents: read
  statuses: write

jobs:
  super-lint:
    uses: miikkak/workflows/.github/workflows/super-linter.yml@b2fba38516c51472610b61160ddaee133eb6c068 # main
    secrets: inherit
    with:
      validate-bash: false
      validate-json: true
