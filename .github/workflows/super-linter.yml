---
name: Lint

on: # yamllint disable-line rule:truthy
  push:
    branches: [main]
  workflow_dispatch:


permissions:
  contents: read
  statuses: write

jobs:
  super-lint:
    uses: miikkak/workflows/.github/workflows/super-linter.yml@27966bd9a329d248775501ebf4dfeb804c90f063 # main
    secrets: inherit
    with:
      validate-bash: false
      validate-json: true
