---
name: Lint

on: # yamllint disable-line rule:truthy
  push:
    branches: [main]
  workflow_dispatch:


permissions:
  contents: read
  statuses: write

jobs:
  super-lint:
    uses: miikkak/workflows/.github/workflows/super-linter.yml@bc94533be449cb1f5d0976594d431dc11386b158 # main
    secrets: inherit
    with:
      validate-bash: false
      validate-json: true
