---
name: Lint

on: # yamllint disable-line rule:truthy
  push:
    branches: [main]
  workflow_dispatch:


permissions:
  contents: read
  statuses: write

jobs:
  super-lint:
    uses: miikkak/workflows/.github/workflows/super-linter.yml@f08ec6064cff32cdaaac64056df1099afcec3101 # main
    secrets: inherit
    with:
      validate-bash: false
      validate-json: true
