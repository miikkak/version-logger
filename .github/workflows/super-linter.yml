---
name: Lint

on: # yamllint disable-line rule:truthy
  push:
    branches: [main]
  workflow_dispatch:


permissions:
  contents: read
  statuses: write

jobs:
  super-lint:
    uses: miikkak/workflows/.github/workflows/super-linter.yml@a12d676d882832ddbbab070f3b58405c3c122d34 # main
    secrets: inherit
    with:
      validate-bash: false
      validate-json: true
