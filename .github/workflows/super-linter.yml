---
name: Lint

on: # yamllint disable-line rule:truthy
  push:
    branches: [main]
  workflow_dispatch:


permissions:
  contents: read
  statuses: write

jobs:
  super-lint:
    uses: miikkak/workflows/.github/workflows/super-linter.yml@1f1f31ffb621afdf34a90c85089cabc3c1d2c59b # main
    secrets: inherit
    with:
      validate-bash: false
      validate-json: true
