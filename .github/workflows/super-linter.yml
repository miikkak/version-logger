---
name: Lint

on: # yamllint disable-line rule:truthy
  push:
    branches: [main]
  workflow_dispatch:


permissions:
  contents: read
  statuses: write

jobs:
  super-lint:
    uses: miikkak/workflows/.github/workflows/super-linter.yml@b847a1b579129b0f1d45456ffb2b1a054f9f406c # main
    secrets: inherit
    with:
      validate-bash: false
      validate-json: true
