---
name: Lint

on: # yamllint disable-line rule:truthy
  push:
    branches: [main]
  workflow_dispatch:


permissions:
  contents: read
  statuses: write

jobs:
  super-lint:
    uses: miikkak/workflows/.github/workflows/super-linter.yml@faf5151059633043890902d27d52ccfbef45d479 # main
    secrets: inherit
    with:
      validate-bash: false
      validate-json: true
