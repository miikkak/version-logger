---
name: Lint

on: # yamllint disable-line rule:truthy
  push:
    branches: [main]
  workflow_dispatch:


permissions:
  contents: read
  statuses: write

jobs:
  super-lint:
    uses: miikkak/workflows/.github/workflows/super-linter.yml@e599eb3588fa60cd265bdb678509a3e61a7f3abe # main
    secrets: inherit
    with:
      validate-bash: false
      validate-json: true
