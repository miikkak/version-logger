---
name: Lint

on: # yamllint disable-line rule:truthy
  push:
    branches: [main]
  workflow_dispatch:


permissions:
  contents: read
  statuses: write

jobs:
  super-lint:
    uses: miikkak/workflows/.github/workflows/super-linter.yml@eea944d066babc3d282b1deb160c583e7f0a0509 # main
    secrets: inherit
    with:
      validate-bash: false
      validate-json: true
