---
name: Lint

on: # yamllint disable-line rule:truthy
  push:
    branches: [main]
  workflow_dispatch:


permissions:
  contents: read
  statuses: write

jobs:
  super-lint:
    uses: miikkak/workflows/.github/workflows/super-linter.yml@20519da379d48fa8a499b4c7b1753a09886fbfb7 # main
    secrets: inherit
    with:
      validate-bash: false
      validate-json: true
