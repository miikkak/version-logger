---
name: Lint

on: # yamllint disable-line rule:truthy
  push:
    branches: [main]
  workflow_dispatch:


permissions:
  contents: read
  statuses: write

jobs:
  super-lint:
    uses: miikkak/workflows/.github/workflows/super-linter.yml@e13cfbf2b04c2c0f3140a88bb26ac0947ac373e5 # main
    secrets: inherit
    with:
      validate-bash: false
      validate-json: true
