---
name: Lint

on: # yamllint disable-line rule:truthy
  push:
    branches: [main]
  workflow_dispatch:


permissions:
  contents: read
  statuses: write

jobs:
  super-lint:
    uses: miikkak/workflows/.github/workflows/super-linter.yml@448cc95551a31fccec0c28b10aa78a9a011d4e15 # main
    secrets: inherit
    with:
      validate-bash: false
      validate-json: true
