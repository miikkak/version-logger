---
name: Lint

on: # yamllint disable-line rule:truthy
  push:
    branches: [main]
  workflow_dispatch:


permissions:
  contents: read
  statuses: write

jobs:
  super-lint:
    uses: miikkak/workflows/.github/workflows/super-linter.yml@aabbdb405cca65c6462f605d44393b4fb90ffba2 # main
    secrets: inherit
    with:
      validate-bash: false
      validate-json: true
