---
name: Lint

on: # yamllint disable-line rule:truthy
  push:
    branches: [main]
  workflow_dispatch:


permissions:
  contents: read
  statuses: write

jobs:
  super-lint:
    uses: miikkak/workflows/.github/workflows/super-linter.yml@3900607f1b5b21d48795cd3824ac80ebab736577 # main
    secrets: inherit
    with:
      validate-bash: false
      validate-json: true
