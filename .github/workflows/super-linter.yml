---
name: Lint

on: # yamllint disable-line rule:truthy
  push:
    branches: [main]
  workflow_dispatch:


permissions:
  contents: read
  statuses: write

jobs:
  super-lint:
    uses: miikkak/workflows/.github/workflows/super-linter.yml@156374d3dfc2dd02fd9b0d0d75a12ea013ecbf02 # main
    secrets: inherit
    with:
      validate-bash: false
      validate-json: true
