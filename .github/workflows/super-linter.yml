---
name: Lint

on: # yamllint disable-line rule:truthy
  push:
    branches: [main]
  workflow_dispatch:


permissions:
  contents: read
  statuses: write

jobs:
  super-lint:
    uses: miikkak/workflows/.github/workflows/super-linter.yml@0de729a83eced92183ba67594ce743daacdbca64 # main
    secrets: inherit
    with:
      validate-bash: false
      validate-json: true
